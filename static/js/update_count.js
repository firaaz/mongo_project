'use strict';

const e = React.createElement;

class UpdateCount extends React.Component {
  constructor(props) {
    super(props);
    this.state = { count: 0 };
  }

  render() {
    ###
  }
}

const domContainer = document.querySelector('#update_count_container');
ReactDOM.render(e(UpdateCounts), domContainer);